# CTA 动画调整:圆角矩形作背景层,动画覆盖两块之上

## 布局调整(核心诉求)
- 两个圆角色块(大块 `--primary-light` + 小块 `#ccefe0`)保留为**纯背景层**,错位关系、圆角、配色不变
- 动画 SVG **不再塞在大块内部**,改为绝对定位覆盖整个视觉区、**叠在两个色块顶层**(z-index 高于色块,范围横跨大块+小块),图形因此有更大施展空间、更有层次感

## 动画内容(光伏主题,不含焊装)
沿用「光伏板+太阳」图形但重新构图适配横跨布局:太阳在左上、两块斜置光伏板分布在大小色块交界处、地平线贯通——描边进场绘制 + 太阳光线呼吸 + 板面光泽扫过(节奏与上一版一致)。

## 实施
1. 调整 `src/components/CtaVisual.vue`:SVG 移出色块,绝对定位铺满 `.cta-visual`(宽约 92%,居中);色块层 `z-index: 0`、SVG 层 `z-index: 1`;reveal 观察器保留
2. Home/Services 接入零改动
3. 验证:build 通过;浏览器目测首页与 Services 页效果,1280/1024/768/375 四档无横向溢出
4. P4 记忆库 + P5 提交推送