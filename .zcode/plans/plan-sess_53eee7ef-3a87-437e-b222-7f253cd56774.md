# 替换 YK-6W 产品图

## 现状
- YK-6W 图片：`src/assets/products/yk-6w_banner.jpg`（14KB，当前与 YK-6B 用的是同一张源图），仅在 `src/data/index.js:50` 一处导入、Services 页展示。
- 用户提供了新图（产线实拍，位于会话图片缓存 `C:\Users\20010811\.zcode\cli\image-cache\...\image-cb4d4d5654d889128d6181a73d19ae08.png`）。

## 改动
1. 用 sharp（项目已有依赖）一次性处理新图：裁掉边缘白边（如有）+ 限宽 800（与产品图管线惯例一致）+ `jpeg({quality:80, mozjpeg:true})`，覆写写入 `src/assets/products/yk-6w_banner.jpg`。
2. 数据与代码零改动（文件名不变，导入引用不动）。
3. 验证：`npm run build` 通过；本地 dev 打开 Services 页选 YK-6W 目测新图正常、无破图。
4. P4/P5：activeContext.md 记一条；提交推送（若 Netlify 已 Stop builds，则需用户手动 Trigger deploy）。

## 边界
- 仅替换 YK-6W；YK-6B 保持现状（二者当前同图，用户未要求动 6B）。