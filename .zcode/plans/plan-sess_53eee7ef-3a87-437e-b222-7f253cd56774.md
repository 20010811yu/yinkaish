# 取消电池分隔线,保留 12 片逐格填充

## 改动(仅 CtaVisual.vue)
1. **删除分隔线**:移除 `panel__cell` 路径元素及其描边动画/样式(模板、no-preference 块、dasharray 组)
2. **填色提前**:分隔线没了,逐格填充紧随边框——每格延迟从 1.8s 提前到 **0.9s**(边框 0-0.9s 画完即开始逐格填,0.9+11×0.07+0.35≈2.6s 填满);支架/地线同步绘制(0-0.9s)不变
3. 染色呼吸层、太阳、色块背景、reduced-motion 静态、进出视口重播机制全部不变

## 验证
浏览器目测(边框画完直接逐格变蓝,中间无分隔线步骤)+ build + 记忆库 + 推送。