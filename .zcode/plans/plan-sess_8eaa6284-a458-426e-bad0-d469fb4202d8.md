## 任务：首页「为什么选择寅铠」板块重做（参照正泰官网样式）

### 新版式（正泰风：左文右图 + 电路连线 + 大数字）

**左右两栏**（≤768px 单列堆叠）：

- **左列**
  1. 标签「核心优势」+ 左对齐大标题「为什么选择寅铠」
  2. **电路连线装饰**（内联 SVG：绿色折线+端点圆点，从标题延伸，正泰电路风）
  3. 标语（新增 i18n `home.advantages.slogan`：zh「一站式自动化解决方案，一站式满足智造需求」/ en 对应）
  4. 说明段（`home.advantages.lead`：zh「构建“上料、加工、检测、码垛”光伏边框生产全流程自动化新生态」/ en 对应）
  5. **大数字统计**（正泰 140+/1850亿 式：大数字+顶部细线+标签，两个）：`±0.1mm` 加工精度 ｜ `98%` 无故障运行率（数据源自画册参数表，i18n 中英）
- **右列**：复用现有 `hero-city.svg` 科技城市插画（白底、无边框），配少量连线节点装饰
- **4 条优势保留**：移到两栏下方，改为一行四列的轻量排布（顶部细线+编号+标题+一句话，去掉白卡片边框阴影，更贴近正泰的轻盈感）；≤768px 单列

### 改动点

1. `src/views/Home.vue`：重写核心优势 section 模板+样式（新增 .adv 两栏 grid、连线 SVG、.adv-stats 大数字、.adv-list 四列轻量排布；删旧 .cards--4 用法）
2. `src/i18n/zh.js` / `en.js`：新增 `home.advantages.slogan`、`lead`、`stat1Value/stat1Label`、`stat2Value/stat2Label`（中英同步）
3. 资产零新增（hero-city.svg 已有）

### 验证与收尾

- `npm run build` + 浏览器 1280/375 两档截图自查（连线装饰、大数字、四列排布、无溢出）
- P4 记忆库（activeContext/progress）；P5 自动 commit/push 附哈希