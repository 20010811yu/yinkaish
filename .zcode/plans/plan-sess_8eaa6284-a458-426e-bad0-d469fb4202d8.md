## 任务：替换首页轮播首图 + 主标题光影增强

### 改动点

1. **替换首图**
   - `D:\OneDrive\桌面\新建文件夹\home.jpg` 拷贝为 `src/assets/banner-home.jpg`
   - `src/components/HeroCarousel.vue`：新增 `import bannerHome from '../assets/banner-home.jpg'`，slides 第一张的 image 换为 bannerHome（原 banner-company.webp 保留文件但不再使用；文案「寅领智造，铠创未来」不变）
2. **主标题光影**：`.slide__title` 现有单层 `drop-shadow(0 0 22px rgba(0,166,81,0.45))` 增强为多层光影——外层绿光晕（更亮更聚）+ 内层深色投影提升立体感，如：
   `filter: drop-shadow(0 0 12px rgba(110,231,168,0.6)) drop-shadow(0 0 34px rgba(0,166,81,0.5)) drop-shadow(0 3px 8px rgba(0,20,10,0.5));`
   渐变文字其余样式不变

### 验证与收尾

- `npm run build` 通过 + 浏览器确认首图已换、标题光影生效
- P4 记忆库：activeContext.md 记录；其余核对
- P5 自动 git add -A → commit → push，附哈希