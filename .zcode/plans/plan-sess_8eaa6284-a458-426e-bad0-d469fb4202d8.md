# 任务：首页新增「服务与合作伙伴」模块（含世界地图 + 源站合作伙伴名单）

## 名单来源（按最新要求）
合作伙伴名单以源网站 http://www.yinkaish.com/ 首页合作伙伴区为准（20 张 logo：`yinkaiweb.oss-cn-shanghai.aliyuncs.com/images/logo/1~20.png`）。
实施第一步：下载 20 张 logo → 逐张读图转写名称（预期含 COMAU/KUKA/ABB/YASKAWA/SIEMENS/EDAG/FFT/首钢/TOWER 等），以此为准建数据，不使用自行拟定的名单。

## 改动点

1. **`src/data/index.js`**：新增 `partners` 常量数组（源站 logo 转写的名称 + 双语 `desc`），导出供 Home 消费
2. **世界地图素材**：下载 Wikimedia 公有领域世界地图 SVG（入库前核验许可标识为公有领域/CC0），内联进 Home.vue 并用品牌浅绿着色
3. **`src/views/Home.vue`** 在核心优势 section 之后、CTA 之前新增模块：
   - 上部两栏：左列 section-tag「服务与合作伙伴」+ 标题 + 说明段 + 电路连线装饰；右列伙伴墙（细边框格子、hover 变绿）
   - 下部通栏：世界地图 + 区域脉冲标记（圆点+同心圆动画+文字标签：亚太区·上海总部 / 欧洲区 / 北美区），复用现有 `[data-reveal]` 入场动画
4. **`src/i18n/zh.js` / `en.js`**：新增 `home.partners.*` 中英双语 key（说明段与区域标签），禁止硬编码

## 规范要点
- 弹性布局：max-width 容器、两栏 `1fr`、地图用 `aspect-ratio`/clamp；网格项 `min-width:0`；≤768 单列降级；四档宽度（1280/1024/768/375）自查无横向滚动
- `prefers-reduced-motion` 降级（脉冲动画关闭）

## 验证与收尾
- `npm run build` 通过；本地 5174 + `/yinkaish/` 路径目检新模块与动画
- P4：更新 activeContext.md / progress.md，输出逐文件检查清单
- P5：`git add -A` → commit（`feat: 首页新增服务与合作伙伴模块(世界地图+源站伙伴名单)`）→ push，附哈希