## 任务：企业精神区块仿截图模块样式调整

### 改动点（仅 `src/views/About.vue`，i18n 不变）

1. **标题同级**：「企业精神」标题改为复用全局 `.section-title` 类（`<h4 class="section-title profile__spirit-title">`），字号/字重与「公司简介」四字完全一致（2.125rem / 700），叠加 `.profile__spirit-title` 仅微调 margin-bottom: 18px
2. **词条放大**：六个精神词条字号从 `clamp(15px, 1.2vw, 18px)` 提升到与图中绿色数字同档的 **`clamp(28px, 2.6vw, 42px)`**（即与上方公司数据数字同一规格），绿色 #00a651 加粗不变
3. **间距**：区块上边距 36→40px，词条行距 16→20px（放大后留呼吸感）

### 验证与收尾

- `npm run build` 通过 + 浏览器确认标题与「公司简介」等大、词条与数据数字等大、sticky 浮动正常、375 无溢出
- P4 记忆库：activeContext.md 更新样式描述；其余核对
- P5 自动 git add -A → commit → push，附哈希