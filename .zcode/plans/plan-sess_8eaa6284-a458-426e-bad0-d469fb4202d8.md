# 任务：降低首页轮播高度

当前轮播高度 `clamp(480px, 55vw, 780px)` 偏高（宽屏顶到 780px，接近满屏）。

## 方案
`HeroCarousel.vue` 轮播高度改为 **`clamp(400px, 40vw, 560px)`**：
- 手机 ≥400px、笔记本（1366px）约 546px、宽屏封顶 560px——比原来矮约 30%，一屏内可看到下方内容

## 验证与收尾
- `npm run build`；预览目检高度与文字排版（标题/副标题仍在画面内）
- P4 记忆库一行；P5 commit + push 附哈希
- 如高度不合意，告知目标值即可再调