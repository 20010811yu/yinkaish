## 任务：企业精神词条字号与数据数字视觉等大

### 背景

图一（精神词条）与图二（数据数字 2004年/150+）当前计算字号相同（均 clamp(28px, 2.6vw, 42px) ≈ 33px@1280），但汉字字形占满整个 em 框（约 0.88em）而数字大写高度仅约 0.72em，同字号下汉字视觉上大一圈——这是用户看到的大小差异来源。

### 改动点（仅 `src/views/About.vue` 一处样式）

`.profile__spirit-item` 字号 `clamp(28px, 2.6vw, 42px)` → **`clamp(21px, 2vw, 32px)`**（≈数字字号的 0.75 档：1280 视口下词条 ≈25.6px，视觉高度 25.6×0.88≈22.5px ≈ 数字 33.3×0.72≈24px，肉眼等大），绿色加粗与其余布局不变。

### 验证与收尾

- `npm run build` 通过 + 浏览器截图整侧栏，自查词条与数字视觉高度一致、375 无溢出
- P4 记忆库：activeContext.md 更新样式参数；其余核对
- P5 自动 git add -A → commit → push，附哈希