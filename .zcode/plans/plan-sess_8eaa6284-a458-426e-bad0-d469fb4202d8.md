## 任务：修复业务与产品页底部 CTA 横带样式缺失

### 原因

上一次重写 Services.vue 时模板保留了 `cta-band` 区块，但 scoped 样式遗漏了 `.cta-band` 定义——导致横带渲染为白底左对齐裸文本（即截图现象）。

### 改动点（仅 `src/views/Services.vue`）

补回 `.cta-band` 样式组：品牌绿渐变背景（135deg #0a5c33→#00a651）、内容水平居中、`padding: clamp(48px, 6vw, 72px) 0`；标题 `clamp(24px, 2.4vw, 30px)`、描述限宽 640px 居中、按钮下方 26px 间距——与首页 CTA、页面整体风格一致。

### 验证与收尾

- `npm run build` + 浏览器截图确认横带恢复绿底居中
- P4：activeContext.md 记录（含根因，防复发）；P5 自动 commit/push