## 任务：替换首页「视觉检测」轮播背景图

### 改动点

1. **图片处理**：`jose-p-ortiz-pArN6HzHShY-unsplash.jpg`（3888×5184 竖图，4.5MB）→ sharp 裁切为横幅比例（约 2560×1400，用 attention 策略聚焦相机主体区域）+ 压缩 q80 → 存为 `src/assets/banner-vision.jpg`（约几百 KB）
2. **`src/components/HeroCarousel.vue`**：`bannerVision` 导入改为 `../assets/banner-vision.jpg`；文案不变（「视觉检测，明察秋毫 / 让每一支边框都值得信赖」）
3. 删除不再使用的 `src/assets/banner-vision.png`

### 验证与收尾

- `npm run build` + 浏览器切到第 3 张确认新背景、标题光影正常、无溢出
- P4 记忆库一行；P5 自动 commit/push 附哈希
- 备注：竖图裁横幅后只保留画面中段（相机主体），如裁切构图不理想可再调整取景位置