# 任务：移除团队模块

1. **路由** `src/router/index.js`：删除 `/team` 路由
2. **导航** `src/components/AppNavbar.vue`：删除「团队」链接项
3. **页面** 删除 `src/views/Team.vue`
4. **数据** `src/data/index.js`：删除 `team` 导出（注意保留 news 内含 "team" 字样的无关文案与 jobs）
5. **i18n** `zh.js` / `en.js`：删除 `nav.team` 与整个 `team.*` 区块（中英同步）
6. 全局 grep 确认无残留 `/team`、`views/Team`、`nav.team` 引用

## 验证与收尾
- `npm run build` 通过；浏览器确认导航无「团队」、直接访问 /team 落回 404/回退页不报错、其余页面正常
- P4 更新 activeContext.md / progress.md；P5 自动 commit + push 附哈希