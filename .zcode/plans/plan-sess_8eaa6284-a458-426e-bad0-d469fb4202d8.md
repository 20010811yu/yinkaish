## 任务概览

五项改动：① 删除 About 页「企业文化」模块 ② 页脚配色改品牌绿 ③ 页脚公司名改全称 ④ 页脚联系方式改用源网站信息（保留邮箱、**不含网址**）⑤ 页脚新增公众号/抖音二维码。含记忆库同步与 git 推送（P4/P5）。

## 一、删除企业文化模块

- `src/views/About.vue`：删除最后一个 section（行 121-131）、`keywords` computed（行 154-158）、`.culture__keywords` 样式（行 537-542）
- `src/i18n/zh.js` / `en.js`：删除 `about.cultureTitle`、`about.cultureDesc`

## 二、页脚配色 → 主题绿

`src/components/AppFooter.vue` 背景 `#12233f`（深藏青）→ 深绿渐变（如 `linear-gradient(180deg, #057842, #04512f)`，取自品牌绿 #00a651 的深色系），同步微调版权条分隔线/文字透明度保证对比度。

## 三、页脚公司全称

品牌区 `$t('brand.short')` → `$t('brand.full')`（上海寅铠精密机械制造有限公司 / Shanghai Yinkai Precision Machinery Manufacturing Co., Ltd.），字号自适应微调。

## 四、联系方式（源站信息 + 保留邮箱，不含网址）

- i18n `contact` 段新增 `fax`（'021-69173483' / '+86-021-69173483'）；`address` 按源站改为「上海市嘉定区安亭镇园区路 567 号」（去邮编）；`phone` 不变；`email` 保留 caolujia@yinkaish.cn（含 `{'@'}` 转义，不动）
- 页脚联系方式列变为：地址 / 电话 / 传真 / 邮箱（**不添加 www.yinkaish.com**）
- 注意 vue-i18n `@` 转义铁律（ERR-001）

## 五、公众号 + 抖音二维码

1. 实现阶段用 `curl` 抓取源站首页与「联系我们/招贤纳士」页原始 HTML，定位二维码 `<img>` URL（源站确认有"扫一扫，关注微信"码），下载到 `src/assets/qrcode/`（wechat.png / douyin.png）
2. **兜底**：若源站只找到微信码没有抖音码，则抖音位先生成占位图并在交付说明中告知用户替换
3. `AppFooter.vue` 新增第四列「关注我们」：两张码（白底圆角卡片约 100px）+ 标签（公众号 / 抖音）
4. 新增 i18n key：`footer.follow`、`footer.wechat`、`footer.douyin`（中英同步）
5. grid 改 4 列（1.4fr 0.9fr 1.3fr 0.9fr 左右），≤768px 单列堆叠

## 六、验证与收尾

- `npm run build` 必须通过；`npm run dev` 浏览器自查页脚与 About 页
- **P4 记忆库同步**（逐文件检查清单随交付输出）：activeContext.md（新焦点/变更）、progress.md（版本行）、errorlog.md（如有新坑）、其余文件逐一核对
- **P5 自动 git**：add -A → commit（`feat: ...`）→ push，交付附提交哈希与远程分支