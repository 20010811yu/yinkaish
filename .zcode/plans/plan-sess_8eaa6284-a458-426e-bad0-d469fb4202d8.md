# 任务：删除独角兽，保留两块绿色圆角背景

1. **`src/views/Home.vue`** 模板：删除 `<svg class="unicorn">` 整块，保留 `.cta__visual` 与两个绿色圆角色块（`cta__block--big/small`）
2. **样式清理**：删除 `.unicorn`、`.unicorn__float`、`.unicorn__wave`、`.unicorn__eye` 及 unicorn-* keyframes 动画样式（色块样式保留；视觉容器高度改由色块撑起，加 `min-height` 保证色块可见）
3. **验证**：`npm run build` + 预览目检（两块绿色背景保留、无独角兽、无残留样式）
4. P4 记忆库一行；P5 commit + push 附哈希