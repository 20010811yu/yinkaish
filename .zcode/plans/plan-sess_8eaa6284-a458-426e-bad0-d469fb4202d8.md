## 任务：取消导航栏选中项的背景底纹

### 改动点（仅 `src/components/AppNavbar.vue` 一处）

`.navbar__link--active` 样式删除 `background: var(--c-primary-light)`（图一中「关于寅铠」的浅绿圆角底纹即来源于此），保留 `color: var(--c-primary)` 与 `font-weight: 600` 作为选中指示；hover 态的浅绿背景不动（仅取消选中态底纹，悬浮反馈保留）。

### 验证与收尾

- `npm run build` 通过 + 浏览器确认导航选中项无底纹、悬浮态仍有反馈
- P4 记忆库：activeContext.md 记一行；其余核对
- P5 自动 git add -A → commit → push，附哈希