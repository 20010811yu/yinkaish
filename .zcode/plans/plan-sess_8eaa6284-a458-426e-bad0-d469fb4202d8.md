## 任务：首页轮播切换间隔改为 3 秒

### 改动点（仅 `src/components/HeroCarousel.vue` 一处）

`:interval="5000"` → **`:interval="3000"`**（3 秒/张），切换动画速度不变。

### 验证与收尾

- `npm run build` + 浏览器确认 3 秒自动切换
- P4 记忆库记一行；P5 自动 commit/push 附哈希