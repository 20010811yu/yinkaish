## 任务：轮播顺序调换 + 切换箭头放大

### 改动点（仅 `src/components/HeroCarousel.vue`）

1. **顺序调换**：slides 数组中第 4、5 张互换 → 新顺序：①新大楼 ②光伏 ③视觉检测 ④**焊装交钥匙（car.jpg）** ⑤**寅铠精神（cooperation）**
2. **箭头放大**：scoped 深度覆盖 `.el-carousel__arrow`（默认 36px）→ 56px 圆钮、图标字号加大、半透明黑底白字（hover 加深），左右各留 16px 边距；移动端略缩（44px）

### 验证与收尾

- `npm run build` + 浏览器确认第 4 张为焊装、第 5 张为寅铠精神、箭头明显变大且可点击
- P4 记忆库一行；P5 自动 commit/push 附哈希