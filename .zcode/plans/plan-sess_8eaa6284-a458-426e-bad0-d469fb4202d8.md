# 任务：修复 ERR-004 —— 地图标记精确化 + 跟随缩放

1. **数据计算坐标**：运行脚本（/tmp/svgmaps/locate.cjs，sharp 已可用）提取 us/eg/in/my/cn 五国 path → 单国渲染 → trim 得包围盒 → 换算 1010×666 视图百分比；美国取本土区（bbox 偏右下），马来西亚取西马半岛（bbox 偏左），上海按中国 bbox 内东经 121°/北纬 31° 比例取点
2. **修正 Home.vue**：五组 `.worldmap__mark--*` 百分比换成计算值；`.worldmap` 容器 `aspect-ratio` 由 1010/560 改为与图片一致的 **1010/666**（% 定位天然随缩放跟随，比例一致后任何宽度都对应正确地理位置）
3. **浏览器 1280/1366 两档截图逐一比对**五个落点
4. **errorlog.md ERR-004** 转 🟢 已解决并补验证结果；P5 commit + push 附哈希