## 任务：YK-OL-2I 查看器悬停暂停自动切换

### 改动点（仅 `src/views/Services.vue`）

- `.detail-fig` 加 `@mouseenter/@mouseleave` 维护 `figHover` 状态
- 自动轮播的 interval 回调中：`figHover` 为真时跳过切换（定时器保持运行，移开鼠标即恢复；与首页节拍一致的 3.5s）

### 验证与收尾

- `npm run build` + 浏览器：悬停图片超过节拍间隔不切换、移开后恢复自动切换；375 无溢出
- P4 记忆库一行；P5 自动 commit/push 附哈希