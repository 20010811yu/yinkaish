## 任务：YK-OL-2I 详情图改为多图轮播（本地图片库，无缩略图）

### 素材

`D:\OneDrive\桌面\网站\image\设备\视觉检测\YK-OL-2I\` 下 5 张图（1.png~5.png，约 1MB/张）→ sharp 压缩（限宽 1200 + palette PNG）后拷入 `src/assets/products/yk-ol-2i/`。

### 改动点

1. **`src/data/index.js`**：YK-OL-2I 产品增加 `images` 数组——用 `import.meta.glob` 按文件名排序导入 5 张图（原单图 `image` 保留作兼容）
2. **`src/views/Services.vue`**：详情区图片改为轻量轮播查看器——
   - 按顺序**单张显示**（`images[idx]`），**无缩略图**
   - 左右切换箭头（样式同轮播放大箭头风格）+ 右下角「idx / 总数」计数
   - 3.5 秒自动轮播（与首页节奏一致），手动切换后重置计时；切换产品时 `idx` 归零
   - 单图产品（其他型号）不显示箭头与计数，直接静态显示
   - 弹性合规：图片 `aspect-ratio 3/2 + object-fit contain`，不破坏布局
3. 其余产品与文案不动

### 验证与收尾

- `npm run build` + 浏览器：切到 YK-OL-2I 验证 5 张依次显示、箭头/计数正常、自动轮播；其他型号详情不受影响；375 无溢出
- P4 记忆库；P5 自动 commit/push 附哈希