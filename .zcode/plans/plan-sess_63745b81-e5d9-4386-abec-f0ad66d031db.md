## 轮播文字科技感设计 + 第二张两行排版

仅改 `src/components/HeroCarousel.vue` 与 i18n 文案结构,图片顺序不变:

1. **主标题科技感字体效果**
   - 字重加大、桌面字号升至约 56px,白色→淡绿渐变文字(background-clip: text)+ 绿色光晕 text-shadow
   - 标题左侧绿色渐变装饰竖线;入场时淡入上移动画(副标题延迟 0.2s)
   - 副标题拉宽字距、细体半透明,上方加渐变细分割线;遮罩左下加深突出文字

2. **第二张标语分两行**(i18n 拆成两段):
   - 第一行:`光伏边框`(大字主标题)
   - 第二行:`非标自动化引领者`(副标题行,字号约为主标题一半)
   - 通过给 slide 数据增加 `titleKey2`(或用换行符)实现,仅第二张生效;英文对应 "PV Frames" / "Pioneer of Custom Automation"

3. **验证与收尾**:`npm run build` 通过 → 浏览器截图确认两页效果 → P4 更新 activeContext → P5 自动提交推送并汇报哈希。