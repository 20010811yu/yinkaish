## 荣誉模块重设计:相册式大图浏览 + 按重要程度排序

替换现有 3 列小卡片网格,改为适合竖版证书阅读的**相册式布局**:

1. **大图查看器(左)**:当前选中证书大幅展示(高约 560px、圆角、阴影、白色衬底),左右切换箭头 + 淡入切换动画;下方显示"当前序号/总数"
2. **缩略图导航(右)**:竖排缩略图列表(当前项高亮描边),点击切换大图;移动端改为横向缩略图条
3. **排序(按重要程度从高到低)**:①荣誉证书合集(综合)→ ②专精特新中小企业 → ③质量管理体系认证(中文)→ ④质量管理体系认证(英文)→ ⑤安全生产标准化三级 → ⑥实用新型专利 → ⑦专利登记簿副本
4. **验证收尾**:build → 浏览器检查切换与移动端 → P4 更新 activeContext → P5 提交推送汇报哈希