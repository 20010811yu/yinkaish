## 轮播高度对比与最优宽度落地

1. 在浏览器中对现网预览临时注入两种高度(780px 与 1040px)分别截图,与当前 520px 对比:评估构图完整性(图片是否过度裁切/拉伸)、文字占比、首屏信息量
2. 选出最优高度写入 `HeroCarousel.vue`(含移动端断点同步),删除临时注入
3. build 验证 + 截图复查 → P4 更新 activeContext → P5 提交推送汇报哈希