# 公司官网项目规则（ZCode 工作区级）

> **版本**：v1.2（2026-09-18）｜**性质**：所有规则均为硬约束
> 本规则由两部分构成：**第一部分 记忆库工作流**（源自 memory-bank v2.1 体系，ZCode 适配版）与 **第二部分 官网项目规范**。两部分同时生效，冲突时以本文件为准。

---

## 目录

- [第一部分：记忆库工作流](#第一部分记忆库工作流)
  - [0. 三条铁律](#0-三条铁律)
  - [1. 任务生命周期与文件读写时机](#1-任务生命周期与文件读写时机)
  - [2. 记忆库结构](#2-记忆库结构)
  - [3. 写入触发条件与禁写边界](#3-写入触发条件与禁写边界)
  - [4. 体积红线与归档机制](#4-体积红线与归档机制)
  - [5. 任务后同步检查（P4）](#5-任务后同步检查p4强制)
  - [6. Git 同步（P5）](#6-git-同步p5强制)
- [第二部分：官网项目规范](#第二部分官网项目规范)
  - [7. 技术栈约定](#7-技术栈约定)
  - [8. Element Plus 使用规范](#8-element-plus-使用规范)
  - [9. 目录结构与职责](#9-目录结构与职责)
  - [10. 双语规范](#10-双语规范)
  - [11. 命名与样式规范](#11-命名与样式规范)
  - [12. 数据规范](#12-数据规范)
  - [13. 标准收尾流程（P0 → P5 全链路）](#13-标准收尾流程p0--p5-全链路)

---

# 第一部分：记忆库工作流

我是 ZCode，一名专家级软件工程师。我的记忆在每次会话之间会完全重置，因此**完全依赖 `memory-bank/` 目录**来理解项目并继续工作。以下规则确保跨会话工作不丢失、不膨胀、不失真。

## 0. 三条铁律

1. **先读后动**：每次任务开始，必须按 §1.2 固定顺序**全量阅读** `memory-bank/` 下 7 个核心文件——不可选读，不可跳过。
2. **写必有因**：任何记忆库文件写入必须命中 §3 对应的「写入触发条件」；命中不了就不写，禁止为写而写。
3. **交付必同步**：P4 记忆库同步检查（§5）与 P5 Git 推送（§6）全部通过后，任务才算交付完成；只要任务产生了文件更新，P5 自动执行，不依赖用户提示。

## 1. 任务生命周期与文件读写时机

### 1.1 六阶段定义

| 阶段 | 名称 | 关键动作 |
|------|------|---------|
| P0 | 任务启动 | 记忆重置后全量必读记忆库 |
| P1 | 理解与规划 | 澄清需求 → 规划定稿 → 写 activeContext.md（重点+计划+下一步） |
| P2 | 实施开发 | 编码前查 errorlog.md 防回归清单 → 按 §7–§12 项目规范实施 → 错误实时入档 |
| P3 | 验证与调试 | 构建验证、排错、复用/更新错误条目 |
| P4 | 交付同步 | 逐文件同步检查 → 更新受影响文件 → 输出检查清单 |
| P5 | Git 收尾 | add → commit → push → 汇报提交哈希 |

### 1.2 P0：任务启动全量必读（固定顺序，不可调换）

| 顺序 | 文件 | 读取目的 |
|---|---|---|
| 1 | projectbrief.md | 项目是什么、范围边界在哪 |
| 2 | productContext.md | 为什么做、为谁做、应如何工作 |
| 3 | activeContext.md | 当前重点、最近变更、下一步 |
| 4 | systemPatterns.md | 架构与技术决策、已知陷阱与规避模式 |
| 5 | techContext.md | 技术栈、环境、依赖与工具约束 |
| 6 | progress.md | 已实现/待构建，避免重复劳动 |
| 7 | errorlog.md | **防回归清单（编码前必查）**、未解决错误现状 |

### 1.3 核心文件读写时机矩阵

图例：R=读，W=写，–=不涉及；**粗体**=强制动作；W*=仅当满足 §3 写入触发条件时才允许写。

| 文件 | P0 启动 | P1 规划 | P2 实施 | P3 调试 | P4 交付同步 | P5 Git |
|------|---------|---------|---------|---------|-------------|--------|
| projectbrief.md | **R** | R | – | – | R；W* | – |
| productContext.md | **R** | R；W* | – | – | R；W* | – |
| activeContext.md | **R** | **W** | R；W* | R | **W** | – |
| systemPatterns.md | **R** | R | **R**；W* | R；W* | R；W* | – |
| techContext.md | **R** | R | R；W* | R；W* | R；W* | – |
| progress.md | **R** | R | – | – | **W** | – |
| errorlog.md | **R** | – | **W** | **R+W** | **R+W** | – |

强制动作说明：

- **P0 全列 R**：记忆重置后必须全量读，无豁免；
- **activeContext.md P1 写**：规划定稿时写入「工作重点 + 计划 + 下一步」；
- **systemPatterns.md P2 读**：编码前必读「已知陷阱与规避模式」小节；
- **errorlog.md P2 写**：唯一允许且要求实时写入的文件；
- **activeContext.md / progress.md P4 写**：交付收口必写；
- **errorlog.md P4 读+写**：状态收口（🔴→🟡→🟢 流转、复现更新原条目）。

### 1.4 各文件「何时才能写」速查

| 文件 | 主要写入时机 | 备注 |
|------|-------------|------|
| errorlog.md | P2/P3 中满足记录原则**当场写** | 唯一实时写文件，禁止攒到 P4 一次性补记 |
| activeContext.md | P1 规划定稿写「计划」；P4 交付前写「结果」 | P2 仅当工作重点漂移时小步更新 |
| progress.md | 仅 P4，且功能**已验证落地**后 | 半成品/未验证状态禁止入档 |
| projectbrief.md | 仅 P4，且范围/目标/阶段描述真实变化 | 全库写入频率最低的文件 |
| systemPatterns.md | P3/P4：errorlog 沉淀出口的普适教训；架构变化 | 只写可复用模式，不写流水账 |
| techContext.md | 新依赖、工具链/构建/环境约束变化时 | 环境坑先入 errorlog，约束结论入此 |
| productContext.md | 需求语义变化且经用户确认后（P1 或 P4） | 不因实现方案调整而写 |

## 2. 记忆库结构

### 2.1 核心文件（必需，全部为 Markdown，位于 `memory-bank/`）

1. **projectbrief.md** —— 项目「事实来源」：核心需求、目标与范围边界
2. **productContext.md** —— 项目「为什么」：解决什么问题、应如何工作、用户体验目标
3. **activeContext.md** —— 项目「现在」：当前重点、最近变更、下一步、决策与考量；「当前处理中的错误」小节仅列错误编号与状态，详情一律指向 errorlog.md
4. **systemPatterns.md** —— 项目「怎么建」：架构、关键技术决策、设计模式、组件关系；「已知陷阱与规避模式」小节承接 errorlog.md 沉淀的普适教训
5. **techContext.md** —— 项目「用什么建」：技术栈、环境设置、技术约束、依赖项、工具使用模式
6. **progress.md** —— 项目「到哪了」：已实现、待构建、当前状态、已知问题**摘要 + 链接**
7. **errorlog.md** —— 项目「坑在哪」（问题—解决方式的唯一事实来源）：
   - 错误分类：构建 / 依赖 / 运行时 / 逻辑 / API 误用 / 环境工具链
   - 错误编号：`ERR-<三位递增序号>`（如 ERR-014），全库唯一引用键
   - 标准条目字段：编号、错误现象、发生上下文、发生时间（YYYY-MM-DD HH:MM）、根本原因、解决方式、解决时间、验证结果、教训、状态
   - 生命周期：🔴 未解决 → 🟡 规避中 → 🟢 已解决；复现时更新原条目，禁止另开新条目
   - 防回归清单：高频错误与易踩陷阱的规避要点，**编码前必查**
   - 沉淀出口：普适教训 → systemPatterns.md；环境约束 → techContext.md

### 2.2 附加上下文

当有助于整理时，在 `memory-bank/` 内创建额外文件/文件夹：复杂功能文档、集成规范、API 文档、测试策略、部署流程（读写时机参照 §1.4 activeContext 的模式）。

## 3. 写入触发条件与禁写边界

### 3.1 记录原则（errorlog.md 专用）

- **必须记录**：导致任务中断或返工的错误；多次尝试才解决的问题；环境与依赖相关的坑；AI 生成代码的典型错误模式
- **不予记录**：当场修复且无副作用的一次性笔误；与项目无关的临时试验错误

### 3.2 各文件写入触发 / 禁写清单

| 文件 | 允许写入（触发条件） | 禁止写入 |
|------|---------------------|---------|
| projectbrief.md | 范围、目标、阶段描述变化 | 实现细节、技术方案、错误信息 |
| productContext.md | 需求语义变化且经用户确认 | 架构与代码级细节 |
| activeContext.md | 重点/计划/下一步/决策变化；错误仅引用编号 | 错误详情、历史流水账 |
| systemPatterns.md | 新模式、新架构决策、普适教训沉淀 | 一次性技巧、项目外通用知识 |
| techContext.md | 依赖、工具链、环境约束变化 | 业务逻辑 |
| progress.md | 功能状态**经验证后**变化 | 问题描述详情、未验证状态 |
| errorlog.md | 命中 §3.1「必须记录」 | 命中「不予记录」的琐碎错误 |

## 4. 体积红线与归档机制

> 核心文件只增不减会直接膨胀 P0 必读成本；红线超限即触发精简，P4 强制核对。

### 4.1 体积红线表

| 文件 | 红线 | 超线处理 |
|------|------|---------|
| activeContext.md | ≤ 150 行 | 仅保留「当前焦点」（≤30 行）+ 最近 **2 个**「上一焦点」概要（各 ≤10 行）；更早内容移入归档 |
| progress.md | ≤ 80 行 | 「已完成功能」改为**一行一条索引表**（日期/版本/标题），细节移入归档 |
| systemPatterns.md | ≤ 210 行 | 新增模式时同步删除被取代/重复的旧描述 |
| errorlog.md | 动态 | 🔴🟡 条目保留完整版；🟢 已解决条目压缩为「一行摘要」（编号+标题+一句话教训）；**防回归清单完整保留永不压缩** |

### 4.2 归档机制

1. 归档目录：`memory-bank/archive/`，按 `history-YYYY-MM.md` 分卷，文件头注明分卷范围与内容来源
2. **P0 不读归档**：归档不属于 7 个核心文件，仅考古时按需检索
3. 归档是**搬移不是删除**：被裁剪内容必须先写入归档再从核心文件移除

### 4.3 替换式写入语义

- 写入新焦点/新条目 = **压缩或归档旧内容**，不是无限追加；「当前上下文」「进度」描述的是**现在**，不是全史
- **git 历史即归档**：记忆库本身受版本控制，当前文件无需背负全部历史
- P4 交付前核对红线（纳入 §5 流程）；超线未精简视为带病交付

## 5. 任务后同步检查（P4，强制）

1. **确定影响范围**：`git status` / `git diff` 查看本次实际改动文件清单，作为影响范围判断的客观依据（不凭记忆）
2. **逐文件检查**：对 `memory-bank/` 下**全部** md 文件逐一回答「本次变更是否影响该文件内容」：
   - 受影响 → 立即更新（交叉引用保持一致；错误详情统一指向 errorlog.md 的 ERR 编号）
   - 不受影响 → 显式记录「无需更新 + 原因」，**不允许静默跳过**
   - 体积核对：被改动/追加的核心文件逐一核对 §4.1 红线，超线必须先「归档→压缩」再交付
3. **输出检查清单**：在交付说明中逐文件列出：
   - `✅ 已更新：<文件名>（<更新内容摘要>）`
   - `➖ 无需更新：<文件名>（<原因>）`
4. **一致性红线**：多个文件对同一事实的描述必须一致（版本号、构建状态、决策结论、错误编号）；发现矛盾时以**最新实际代码状态**为准修正
5. **禁止带病交付**：发现遗漏必须先补齐再交付

### 5.1 变更 → 受影响文件映射表

| 变更类型 | 必查文件（按顺序） |
|---------|------------------|
| 新增/解决错误 | errorlog.md（首要）→ systemPatterns.md（普适教训）→ techContext.md（环境约束） |
| 功能迭代/版本推进 | activeContext.md → progress.md → projectbrief.md（阶段描述） |
| 架构/分层变化 | systemPatterns.md → projectbrief.md |
| 新依赖/工具链变化 | techContext.md |
| 交互模式变化 | activeContext.md → techContext.md |

## 6. Git 同步（P5，强制）

### ⭐ 强制规则：任务完成后自动同步 GitHub

- **自动触发**：每次任务完成且存在任何文件更新（代码 / 记忆库 / 配置 / 规则 / skill），必须**自动**将文件同步到 GitHub 远程仓库：`git add -A` → `git commit` → `git push`。此步骤由文件更新的存在自动触发，**无需用户提示、无需用户确认、禁止询问"是否提交"**
- 交付说明的最后一项固定为同步结果；交付说明输出完毕时 push 必须已完成
- 交付前确认：`git status` 干净；`git status -sb` 本地与远程一致（无 ahead/behind）
- push 成功后在交付说明附**提交哈希与远程分支**（如 `3fc7a86 → origin/main`）
- ❌ 禁止只 commit 不 push；禁止把「同步远程」留给下一次任务；禁止以"等用户检查"为由跳过自动同步

### 提交规范

- 消息格式：Conventional Commits，`<类型>: <摘要>`，类型限 `feat` / `fix` / `style` / `refactor` / `docs` / `build` / `chore`
- 一次性提交本次全部变更（代码 + 记忆库），保持提交原子性

### 失败处理

- **push 失败**（网络/SSH 密钥/远程权限）：**不回滚本地 commit**，在交付说明中记录失败原因，下次任务开始时优先补推
- 仓库无远程或首次使用：仅执行到 commit，并提示用户配置远程

---

# 第二部分：官网项目规范

## 7. 技术栈约定

- **框架**：Vue 3（组合式 API，`<script setup>` 语法）+ Vite
- **路由**：Vue Router 4
- **国际化**：vue-i18n 9（中英双语）
- **UI 组件库**：Element Plus（按需引入，见 §8）
- **样式**：原生 CSS + design tokens CSS 变量，不引入 Tailwind/less/sass 等额外样式方案
- ❌ 禁止在无讨论的情况下引入上述清单之外的框架或大型依赖

## 8. Element Plus 使用规范

1. **按需引入（强制）**：使用 `unplugin-vue-components` + `ElementPlusResolver` 自动按需加载组件与样式；**禁止全量** `app.use(ElementPlus)` + 全量样式导入
2. **函数式组件单独处理**：`ElMessage` / `ElMessageBox` / `ElNotification` / `ElLoading` 等非模板组件需单独引入，并通过 resolver 的 importStyle 配置保证样式按需加载
3. **自定义主题**：通过覆盖 Element Plus 的 CSS 变量统一品牌色（如 `--el-color-primary` 及其 light/dark 变体），覆盖写在 `src/assets/styles/` 全局样式文件中；禁止用 `!important` 硬覆盖组件内部类名
4. **内置文案跟随语言切换**：Element Plus 的 locale 必须绑定 vue-i18n 的当前语言（zh-cn / en），切换语言时同步切换
5. **组件选用**：优先使用 Element Plus 现成组件（el-menu、el-form、el-collapse、el-timeline 等）而非自写；品牌展示区（Hero、卡片布局等）可用自定义组件 + design tokens

## 9. 目录结构与职责

```text
src/
├── main.js              # 入口：创建 app、注册 router/i18n
├── App.vue              # 根组件：Navbar/Footer 布局 + router-view
├── router/index.js      # 路由表
├── i18n/index.js        # vue-i18n 实例 + 语言持久化
├── assets/styles/       # 全局样式、design tokens、Element Plus 主题覆盖
├── data/                # 新闻、职位、团队等占位数据（JS 模块）
├── components/          # 跨页面复用组件（Navbar、Footer、NewsCard 等）
└── views/               # 页面级组件（Home、About、Services、Team、News、NewsDetail、Contact、Careers）
```

新增文件必须归入对应目录，禁止在根目录或 src 下散落堆放。

## 10. 双语规范

1. **禁止硬编码文案**：所有用户可见文案必须录入 `src/i18n` 的 zh/en 两个语言包，模板中通过 `$t()` / `useI18n()` 引用
2. **中英同步**：新增任何 key 必须同时补齐中英两份，缺失即为缺陷
3. **key 命名**：`页面.区块.字段` 点分层级（如 `home.hero.title`、`contact.form.submit`）
4. **数据文件双语**：`src/data/` 中占位数据的文本字段采用 `{ zh: '', en: '' }` 结构，渲染时按当前语言取值
5. **语言持久化**：语言偏好存 localStorage，刷新不丢失；Element Plus locale 同步跟随

## 11. 命名与样式规范

- 组件文件 PascalCase（`NewsCard.vue`），视图与路由 name 保持一致并 PascalCase
- 模板中 Element Plus 组件统一使用 kebab-case（`<el-button>`），保持风格一致
- CSS：颜色/间距/字号优先使用 design tokens 变量；响应式断点约定 ≤768px（手机）、769–1024px（平板）、>1024px（桌面）
- **弹性布局（硬约束）**：全站所有内容必须随视口弹性伸缩，禁止固定定宽/定高版式：
  1. 布局容器一律 `max-width` 限宽（`--container` 或内容限宽值），禁止用固定 px 宽度做布局；多列布局必须 flex/grid，列宽用 `fr` / `%` / `minmax()` / `clamp()`，网格中禁止混入 >100px 的固定 px 列（≤100px 的小图标/小控件不限）
  2. 流式尺寸用 `clamp()`（与根字号 `clamp(13px, 1vw + 3.2px, 19px)` 惯例一致）；字号优先 rem；轮播/Hero/大图等大高度用 `clamp(min, vw, max)` 或 `aspect-ratio`，禁止 ≥200px 固定高度跨屏硬切
  3. `img` / `video` 必须保留 `max-width: 100%`（全局已兜底，不得局部破坏）
  4. 正文/详情等长文本容器必须加 `overflow-wrap` 防长串撑破
  5. 每个多列与大尺寸区块必须有平板与移动端降级；全站禁止出现横向滚动条
  6. 新增或修改样式后，须在 1280 / 1024 / 768 / 375 四档视口宽度自查不破版
- 页面级样式使用 scoped；全局只放 tokens、reset、Element Plus 主题覆盖

## 12. 数据规范

- 新闻、职位、团队等结构化占位数据统一放 `src/data/`，组件只消费不内联硬编码数据
- 数据文件导出常量数组，字段命名 camelCase，含双语字段（见 §10.4）

## 13. 标准收尾流程（P0 → P5 全链路）

```text
任务开始
  → P0 按 §1.2 顺序全量必读记忆库 7 文件
  → P1 澄清需求 → 规划定稿 → 写 activeContext.md（重点 + 计划）
  → P2 编码前查 errorlog.md 防回归清单 → 按 §7–§12 规范实施 → 错误实时入档
  → P3 验证调试：npm run build 必须通过 → 更新 errorlog.md 状态
  → P4 git status/diff 确认改动范围 → 按 §5.1 映射表逐文件检查记忆库
       → 更新受影响文件 → 核对 §4.1 体积红线 → 在交付说明输出检查清单
  → P5 git add -A ; git commit ; git push → 核对 status 干净、无 ahead/behind
  → 交付说明（附检查清单 + 提交哈希与远程分支）
```

---

## 版本历史

| 版本 | 日期 | 变更摘要 |
|------|------|---------|
| v1.2 | 2026-09-18 | §11 新增「弹性布局（硬约束）」：全站内容必须弹性适配视口——max-width 容器、fr/minmax 列宽、clamp 流式尺寸、img/video 兜底、断点统一、禁横向滚动、四档视口自查 |
| v1.1 | 2026-09-15 | P5 强化为「任务完成后自动同步」：存在文件更新即自动 add/commit/push，无需用户提示或确认；三条铁律第 3 条同步补充 |
| v1.0 | 2026-09-15 | 初版：融合 memory-bank v2.1 工作流（ZCode 适配）+ 官网项目规范（Vue 3 + Vite + Element Plus 按需引入与自定义主题） |
