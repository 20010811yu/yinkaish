# activeContext.md — 当前上下文

## 当前焦点(2026-09-16)
首期初始化:从零搭建"银凯科技"双语企业官网。

## 计划
1. ✅ 初始化 memory-bank 7 文件 + AGENTS.md 入库
2. Vite 脚手架 + 依赖(vue-router、vue-i18n、element-plus 按需)
3. 路由 8 页 + i18n zh/en + design tokens + Navbar/Footer
4. data 占位数据 + 各页面实现 + 响应式
5. P3 build 验证 → P4 记忆库同步 → P5 自动 push

## 下一步
执行脚手架初始化(npm create vite)。

## 决策与考量
- 品牌名占位"银凯科技",主色蓝色系;表单仅前端提示;NewsDetail 用 `/news/:id` 动态路由。
